== Convoluted Aural Net / Beep Learning

Install ARSS and ImageMagick to create new testing data.

The Python script contains a boolean flag which may be toggled to allow additional training of the model.
